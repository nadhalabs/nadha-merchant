export const API=import.meta.env.VITE_API_URL||'/api';
export const token=()=>localStorage.getItem('token');
export async function api<T>(path:string,options:RequestInit={}):Promise<T>{const url=API+path;let r:Response;try{r=await fetch(url,{...options,headers:{'Content-Type':'application/json',...(token()?{Authorization:`Bearer ${token()}`}:{}) ,...options.headers}})}catch(error){if(import.meta.env.DEV)console.error('API network/CORS error',{url,error});throw error}if(!r.ok){const body=await r.json().catch(()=>({detail:'Request failed'}));if(import.meta.env.DEV)console.error('API response error',{url,status:r.status,body});throw new Error(body.detail||'Request failed')}return r.status===204?undefined as T:r.json()}
export const money=(value:string|number|undefined)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(Number(value||0));
