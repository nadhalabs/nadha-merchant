import{FormEvent,useState}from'react';import{api}from'../api/client';import type{Shop}from'../App';export function NadhaAI({shop}:{shop:Shop}){const[msg,setMsg]=useState('');async function ask(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api<{message:string}>(`/shops/${shop.id}/ai/ask`,{method:'POST',body:JSON.stringify({question:f.get('question'),language:document.documentElement.lang||'en'})});setMsg(r.message)}catch{setMsg("Nadha AI isn't enabled for this account yet.")}}return <section className="card"><h2>Nadha AI</h2><p>Your business assistant can analyze your sales, പറ്റ്, expenses and Diary.</p>{shop.ai_enabled?<form onSubmit={ask}><input name="question" placeholder="Ask about your business…" required/><button className="primary">Ask</button></form>:<button className="secondary">Unlock Nadha AI</button>}{msg&&<p>{msg}</p>}</section>}
