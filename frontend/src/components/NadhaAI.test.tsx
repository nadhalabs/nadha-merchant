import{fireEvent,render,screen}from'@testing-library/react';import{describe,expect,it}from'vitest';import{NadhaAI}from'./NadhaAI';
describe('Nadha AI entitlement',()=>{it('opens the unlock sheet for a locked shop',()=>{render(<NadhaAI shop={{id:'shop-1',name:'Test',timezone:'Asia/Kolkata',ai_enabled:false}}/>);fireEvent.click(screen.getByRole('button',{name:'Unlock Nadha AI'}));expect(screen.getByRole('dialog')).toBeTruthy();expect(screen.getByRole('heading',{name:'Unlock Nadha AI'})).toBeTruthy();expect(screen.getByText('Nadha AI is an optional paid add-on.')).toBeTruthy()})})
