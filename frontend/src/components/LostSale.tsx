import{FormEvent,useState}from'react';import{api}from'../api/client';import type{Shop}from'../App';export function LostSale({shop}:{shop:Shop}){const[open,setOpen]=useState(false),[saved,setSaved]=useState(false);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);await api(`/shops/${shop.id}/lost-sales`,{method:'POST',body:JSON.stringify({requested_product:f.get('requested_product'),quantity:f.get('quantity')||null,note:f.get('note')||null,occurred_at:new Date().toISOString()})});setSaved(true);setOpen(false)}return <div className="lost-action"><button onClick={()=>setOpen(!open)}>Customer asked — not available</button>{saved&&<small>✓ Request remembered</small>}{open&&<form className="card" onSubmit={save}><label>What did they ask for?<input name="requested_product" required autoFocus/></label><label>Quantity (optional)<input name="quantity" inputMode="decimal"/></label><label>Note<input name="note"/></label><button className="primary">Remember request</button></form>}</div>}
