services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nadha
      POSTGRES_PASSWORD: nadha
      POSTGRES_DB: nadha_shop
    ports: ["5432:5432"]
    volumes: ["postgres_data:/var/lib/postgresql/data"]
volumes:
  postgres_data:
